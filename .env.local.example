# Copy this file to .env.local and fill in the real key.
# .env.local is gitignored — never commit secrets.
#
# Get a key from https://openrouter.ai/keys (free tier sufficient for one
# elderly user; ~$30/mo budget per NFR-2a).

EXPO_PUBLIC_OPENROUTER_API_KEY=sk-or-v1-REPLACE_ME
